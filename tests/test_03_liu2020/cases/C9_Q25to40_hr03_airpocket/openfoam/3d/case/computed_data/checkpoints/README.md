# Checkpoint archive slot

Place future solver checkpoint tarballs here (Git LFS), e.g.:

```text
processor_latest_T8.2289420474.tar.xz
postProcessing_phase2_partial.tar.xz
log.full.xz
```

Do not leave resume-critical state only on the VM disk.
