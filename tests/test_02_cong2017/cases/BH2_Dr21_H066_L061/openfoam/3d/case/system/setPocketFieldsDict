FoamFile
{
    format      ascii;
    class       dictionary;
    object      setPocketFieldsDict;
}

defaultFieldValues ();

regions
(
    // This interface coincides with the conformal x=5.98 m valve plane, so
    // full-cell assignment does not stair-step the gas/water boundary.
    boxToCell
    {
        box (5.98 -1 -1) (6.60 1 1);
        fieldValues
        (
            volScalarFieldValue alpha.water 0
        );
    }
);
