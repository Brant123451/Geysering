FoamFile
{
    format      ascii;
    class       dictionary;
    object      decomposeParDict;
}

numberOfSubdomains 4;
method              scotch;

constraints
{
    valveOnOneProcessor
    {
        type        singleProcessorFaceSets;
        sets        ((valvePatchFaces -1));
        enabled     true;
    }
}
