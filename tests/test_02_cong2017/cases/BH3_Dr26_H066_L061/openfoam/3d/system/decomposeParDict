FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      decomposeParDict;
}

numberOfSubdomains 4;
method simple;

simpleCoeffs
{
    // Deterministic axial cuts minimize processor faces in the long pipe.
    n       (4 1 1);
    delta   0.001;
}
