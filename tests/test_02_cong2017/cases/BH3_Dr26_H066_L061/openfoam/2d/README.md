# BH3 OpenFOAM 2-D (fine mesh)

Paper-length/diameter vertical-plane `compressibleInterFoam` case for Cong et al. (2017) Run **B-H3**.

See `HANDOFF.md` for status, ETA, and limitations (planar vs circular area ratio).

```bash
./Allrun
```

Runtime case: `/tmp/bh3-2d-study/base_nominal_2d`.
