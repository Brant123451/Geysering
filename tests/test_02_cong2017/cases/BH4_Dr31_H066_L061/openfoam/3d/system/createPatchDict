FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      createPatchDict;
}

pointSync false;

patches
(
    {
        name walls;
        patchInfo { type wall; }
        constructFrom patches;
        patches (walls);
    }
    {
        name closedEnd;
        patchInfo { type wall; }
        constructFrom patches;
        patches (closedEnd);
    }
    {
        name reservoir;
        patchInfo { type patch; }
        constructFrom patches;
        patches (reservoir);
    }
    {
        name atmosphere;
        patchInfo { type patch; }
        constructFrom patches;
        patches (atmosphere);
    }
);
