#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
scratch_root="${BH6_2D_SCRATCH:-/tmp/bh6-2d-study}"
target="$scratch_root/paper_tau0p2_areaeq"
case "$target" in
    "$scratch_root"/paper_tau0p2_areaeq) ;;
    *) echo "Refusing unsafe target: $target" >&2; exit 2 ;;
esac
rm -rf -- "$target"
echo "REMOVED $target"
