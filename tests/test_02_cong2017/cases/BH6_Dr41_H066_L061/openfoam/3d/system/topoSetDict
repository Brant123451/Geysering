/*--------------------------------*- C++ -*----------------------------------*\
| OpenFOAM v2512                                                        |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name    valveCells;
        type    cellSet;
        action  new;
        source  boxToCell;
        box     (5.975 -0.026 -0.026) (5.985 0.026 0.026);
    }
    {
        name    valveZone;
        type    cellZoneSet;
        action  new;
        source  setToCellZone;
        set     valveCells;
    }
);

// ************************************************************************* //
